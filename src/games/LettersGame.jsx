// LettersGame — placeholder. See poc/letters.js for full logic to port.
export default function LettersGame({ onWin }) {
  return <div className="game-stub">🔤 Letters — port from poc/letters.js</div>;
}
