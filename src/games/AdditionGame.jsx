// AdditionGame — placeholder. See poc/addition.js for full logic to port.
export default function AdditionGame({ onWin }) {
  return <div className="game-stub">➕ Addition — port from poc/addition.js</div>;
}
