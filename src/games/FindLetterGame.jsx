// FindLetterGame — placeholder. See poc/findletter.js for full logic to port.
export default function FindLetterGame({ onWin }) {
  return <div className="game-stub">🔍 Find Letter — port from poc/findletter.js</div>;
}
