/**
 * 12 badge definitions — port of poc/achievements.js.
 * Each badge has: id, emoji, rarity, check(profile, session).
 */
export { BADGES, RARITY_COLOR, checkAchievements, getBadgeById } from '../../poc/achievements.js';
